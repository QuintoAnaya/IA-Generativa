# --- Nucleo del sistema (Componente 1, obligatorio) ---
# El sistema corre end-to-end SIN estas dos si no hay API key (modo simulado),
# pero se recomienda instalarlas para usar Gemini de verdad.
google-generativeai>=0.5.0

# --- Componente 2: generacion de la referencia visual ---
Pillow>=10.0.0

# --- Interfaz para usuarios no tecnicos ---
streamlit>=1.30.0

# --- Generacion de imagen de referencia sintetica (opcional) ---
# Requiere GPU + descarga del modelo desde HuggingFace (varios GB). Si no estan
# instalados, o no hay GPU, el sistema cae automaticamente a un placeholder
# (ver oncobridge/utils/image_gen.py) y el resto del pipeline sigue funcionando.
# Descomentar si vas a correr esto en un entorno con GPU (ej. Google Colab):
# torch>=2.1.0
# diffusers>=0.27.0
# transformers>=4.38.0
# accelerate>=0.27.0
