# ==== Dependencias de OncoBridge AI ====
# Instalar con:  pip install -r requirements.txt

# Modelo generativo (Gemini). El sistema funciona sin esto en modo simulado,
# pero es necesario para usar el LLM real.
google-generativeai>=0.7.0

# Carga de variables de entorno desde .env
python-dotenv>=1.0.0

# --- Opcionales: solo para el MODO IMAGEN REAL del Componente 2 ---
# Descomentar si se quiere inspeccionar imagenes reales (DICOM / PNG / JPG).
# pydicom>=2.4.0
# Pillow>=10.0.0
